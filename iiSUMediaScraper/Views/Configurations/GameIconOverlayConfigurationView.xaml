<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="iiSUMediaScraper.Views.Configurations.GameIconOverlayConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:media="using:CommunityToolkit.WinUI.Media"
             xmlns:ui="using:CommunityToolkit.WinUI"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             xmlns:configurationmodels="using:iiSUMediaScraper.Models.Configurations"
             xmlns:configurationviewmodels="using:iiSUMediaScraper.ViewModels.Configurations"
             xmlns:controls1="using:iiSUMediaScraper.Controls"
             xmlns:converters="using:CommunityToolkit.WinUI.Converters"
             xmlns:customconverters="using:iiSUMediaScraper.Converters"
             xmlns:datatemplateselectors="using:iiSUMediaScraper.DataTemplateSelectors"
             xmlns:viewmodels="using:iiSUMediaScraper.ViewModels"
             xmlns:triggers="using:CommunityToolkit.WinUI"
             xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="using:iiSUMediaScraper.Behaviors"
             xmlns:local="using:iiSUMediaScraper.Views.Configurations"
             xmlns:imageex="using:ImageEx"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <customconverters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />

        <customconverters:BoolNegationConverter x:Key="BoolNegationConverter" />

        <customconverters:PathToValidImagePathConverter x:Key="PathToValidImagePathConverter" />

        <customconverters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

        <customconverters:InverseNullToVisibilityConverter x:Key="InverseNullToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Background="{x:Bind Background, Mode=OneWay}"
          BorderBrush="{x:Bind BorderBrush, Mode=OneWay}"
          BorderThickness="{x:Bind BorderThickness, Mode=OneWay}"
          CornerRadius="{x:Bind CornerRadius, Mode=OneWay}"
          Padding="{x:Bind Padding, Mode=OneWay}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <imageex:ImageEx Stretch="Fill"
                         Height="140"
                         Width="140"
                         Margin="0,0,16,0"
                         Source="{x:Bind ViewModel.Path, Mode=OneWay, Converter={StaticResource PathToValidImagePathConverter}}" />
        <StackPanel Grid.Column="1">
            <Grid Margin="0,0,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBox Header="Path"
                         Text="{x:Bind ViewModel.Path, Mode=TwoWay}"
                         Margin="0,0,16,0" />
                <Button Content="Pick File"
                        Grid.Column="1"
                        VerticalAlignment="Bottom"
                        Click="Button_Click" />
            </Grid>
            <controls1:CustomComboBox Header="Platform"
                                      Width="300"
                                      Grid.Column="1"
                                      SelectedItem="{x:Bind ViewModel.SelectedPlatform, Mode=TwoWay}"
                                      ItemsSource="{x:Bind ViewModel.PlatformConfigurations, Mode=OneWay}"
                                      DisplayMemberPath="Name"
                                      Margin="0,0,16,0" />
        </StackPanel>

        <Button Grid.Column="2"
                Height="32"
                Width="32"
                Padding="0"
                VerticalAlignment="Top"
                Command="{x:Bind ViewModel.RequestRemoveCommand, Mode=OneWay}">
            <Viewbox Height="14">
                <SymbolIcon Symbol="Delete" />
            </Viewbox>
        </Button>
    </Grid>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="iiSUMediaScraper.Views.Configurations.SpecificPlatformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="using:iiSUMediaScraper.Views.Configurations"
             xmlns:configurationviewmodels="using:iiSUMediaScraper.ViewModels.Configurations"
             xmlns:customconverters="using:iiSUMediaScraper.Converters"
             xmlns:imageex="using:ImageEx"
             mc:Ignorable="d">

    <UserControl.Resources>
        <customconverters:InverseStringToVisibilityConverter x:Key="InverseStringToVisibilityConverter" />

        <customconverters:PathToValidImagePathConverter x:Key="PathToValidImagePathConverter" />

        <customconverters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Background="{x:Bind Background, Mode=OneWay}"
          BorderBrush="{x:Bind BorderBrush, Mode=OneWay}"
          BorderThickness="{x:Bind BorderThickness, Mode=OneWay}"
          CornerRadius="{x:Bind CornerRadius, Mode=OneWay}"
          Padding="{x:Bind Padding, Mode=OneWay}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Border Background="{ThemeResource SystemFillColorNeutralBrush}"
                Height="60"
                Width="60"
                Margin="0,0,16,0"
                CornerRadius="4"
                Visibility="{x:Bind ViewModel.IconPath, Mode=OneWay, Converter={StaticResource InverseStringToVisibilityConverter}}">
            <TextBlock Text="{x:Bind ViewModel.Code, Mode=OneWay}"
                       Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="22" />
        </Border>
        <imageex:ImageEx Stretch="Fill"
                         Height="60"
                         Width="60"
                         Margin="0,0,16,0"
                         Source="{x:Bind ViewModel.IconPath, Mode=OneWay, Converter={StaticResource PathToValidImagePathConverter}}"
                         Visibility="{x:Bind ViewModel.IconPath, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}" />
        <TextBlock Text="{x:Bind ViewModel.PlatformConfiguration.Name, Mode=OneWay}"
                   FontSize="18"
                   VerticalAlignment="Center"
                   Grid.Column="1"
                   Margin="0,0,16,0" />
        <CheckBox IsChecked="{x:Bind ViewModel.IsSelected, Mode=TwoWay}"
                  Grid.Column="2"
                  MinWidth="0" />
    </Grid>
</UserControl>
